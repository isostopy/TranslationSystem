using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OtraPrueba : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
		Debug.Log("Otra prueba");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
